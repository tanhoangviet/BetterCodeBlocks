{
  "name": "better-code-blocks",
  "version": "1.0.0",
  "description": "Syntax highlighted code blocks and file viewer for Kettu/Bunny",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "deploy": "node build.mjs && node deploy.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
